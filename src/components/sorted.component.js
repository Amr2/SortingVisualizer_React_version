export default function Sorted(props){
    return(
        <>
            {/* <h1>Sorted state</h1>
            <h3>{props.Columns_arr.map(( elem, indx)=> <span key={"asdmn"+elem.value + indx + "asdasdzxczx"}> {elem.value}  </span>)} ➡ <span>{props.sort_fun(props.Columns_arr)[1].map((elem,indx)=> <span key={elem.value+"asd"+indx}> {elem.value}  </span>)}</span></h3> */}
        </>
    
    )
}

// key={elem}